import Header from "@/components/Header";

export const metadata = {
    title: "Home || Homez",
};

const Home = () => {
    return (
        <div>
            <Header />
            Home
        </div>
    )
}

export default Home